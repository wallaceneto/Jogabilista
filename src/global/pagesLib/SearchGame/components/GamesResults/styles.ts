import {StyleSheet} from 'react-native';

const useStyles = () => {
  return StyleSheet.create({
    container: {
      flex: 1,
    },
    loading: {
      marginTop: 80,
    },
  });
};

export default useStyles;