import {StyleSheet} from 'react-native';

const useStyles = () => {
  return StyleSheet.create({
    filtersContainers: {
      marginHorizontal: 12,
      marginTop: 12,
    },
    checkbox: {
      marginRight: 8,
      marginBottom: 16,
    },
  });
};

export default useStyles;